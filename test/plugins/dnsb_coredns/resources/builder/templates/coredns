{
    "auth": {
        "command": "coredns -conf /etc/coredns/Corefile",
        "volumes": [
            "resource:/configs/coredns_auth_base.conf:/etc/coredns/Corefile:rw"
        ]
    },
    "forwarder": {
        "command": "coredns -conf /etc/coredns/Corefile",
        "volumes": [
            "resource:/configs/coredns_forwarder_base.conf:/etc/coredns/Corefile:rw"
        ]
    },
    "recursor": {
        "command": "coredns -conf /etc/coredns/Corefile",
        "volumes": [
            "resource:/configs/coredns_recursor_base.conf:/etc/coredns/Corefile:rw"
        ]
    }
}